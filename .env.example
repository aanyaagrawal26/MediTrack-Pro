# Copy this file to ".env" when you are ready to connect a real database.
# The app runs fully on localStorage by default, so this is OPTIONAL for the demo.

# PostgreSQL connection string used by Prisma.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/meditrack?schema=public"
